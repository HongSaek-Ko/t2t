<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.t2t.mem.repository.MemberMapper">
    <!-- 회원 1명 정보 조회 -->
    <select id="findByID" resultType="org.t2t.mem.dto.MemberDTO">
        SELECT * FROM MEM WHERE USRID=#{usrId}
    </select>
    <!-- 회원 전체 조회 -->
    <select id="findList" resultType="org.t2t.mem.dto.MemberDTO">
        SELECT * FROM MEM
    </select>
    <update id="updateMem">
        UPDATE MEM
            SET NM=#{nm},
            EMAIL=#{email},
            INTRO=#{intro}
        WHERE
            USRID = #{usrId}
    </update>
    <!-- 회원 탈퇴 -->
    <delete id="deleteMem">
        DELETE FROM MEM
        WHERE USRID = #{usrId}
    </delete>
    <!-- id, pw 일치여부 확인 -->
    <select id="idPwChecking" resultType="org.t2t.mem.dto.MemberDTO">
        SELECT * FROM MEM
        WHERE USRID=#{usrId} AND PASSPW=#{passwd}
    </select>
    <!-- 마일리지 충전 / 환전 -->
    <insert id="updateMile" parameterType="org.t2t.mem.dto.MileDTO">
        UPDATE MILES
        SET POINT =
        <choose>
            <when test="option == 'TRD01'">
                #{point} + POINT
            </when>
            <otherwise>
                POINT - #{point}
            </otherwise>
        </choose>
        WHERE
        USRID = #{usrId}
    </insert>

    <select id="selectMileTotal" resultType="org.t2t.mem.dto.MileDTO" parameterType="string">
        SELECT
            MI.USRID
            ,MI.POINT
            ,MI.REGDT
            ,MI.LASTDT
        FROM
            MEM M LEFT OUTER JOIN MILES MI
            ON M.USRID = MI.USRID
        WHERE
            M.USRID =#{usrId}
    </select>
    <select id="selectComplaintList" parameterType="string" resultType="org.t2t.mem.dto.ComplaintDTO">
        SELECT
            C.*
        FROM PRD R LEFT OUTER JOIN CMP C
        ON R.PRDID = C.PRDID
        WHERE
            R.USRID = #{usrId}
    </select>
    <!-- 나의 구매내역 -->
    <select id="selectOrderList">
        SELECT O.*
        FROM ORD O JOIN PRD D ON O.prdId = D.prdId
        LEFT OUTER JOIN MEM M ON O.usrId = M.usrId
        WHERE O.USRID=#{USRID};
    </select>
    <!-- 나의 과거거래내역 (판매중지, 매진) -->
    <select id="selectPastList" resultType="org.t2t.prd.dto.ProductDTO">

    </select>
    <!-- 나의 현재거래내역 -->
    <select id="selectCurList">

    </select>


</mapper>